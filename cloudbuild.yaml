steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/$PROJECT_ID/My First Project:$SHORT_SHA', '.']
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', 'gcr.io/$PROJECT_ID/My First Projecto:$SHORT_SHA']
- name: 'gcr.io/cloud-builders/gcloud'
  args: ['beta', 'run', 'deploy', 'My First Project', '--image=gcr.io/$PROJECT_ID/My First Project:$SHORT_SHA', '--region=us-central1','--platform=managed','--allow-unauthenticated']
